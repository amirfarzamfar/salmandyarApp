I will implement dynamic vital signs display, a history list, and a chart in the patient portal.

### Plan
1.  **Refactor `HealthSnapshot` Component**:
    *   Modify `src/components/portal/health-snapshot.tsx` to fetch real data using `patientService.getVitals`.
    *   Accept `patientId` as a prop.
    *   Display the **latest** recorded vital signs (Blood Pressure, Heart Rate, Oxygen, Temperature).
    *   Show trend indicators based on the last two records.

2.  **Create `VitalSignsChart` Component**:
    *   Create `src/components/portal/vital-signs-chart.tsx`.
    *   Use `recharts` to display a line chart of vital signs history.
    *   Allow users to switch between different metrics (e.g., Blood Pressure vs Heart Rate).

3.  **Create `VitalSignsHistory` Component**:
    *   Create `src/components/portal/vital-signs-history.tsx`.
    *   Display a tabular list of vital signs history with a beautiful UI.
    *   Include columns for Date, Time, BP, Heart Rate, Temperature, O2, and Recorder Name.

4.  **Update `PortalPage`**:
    *   Update `src/app/portal/page.tsx` to integrate the new components.
    *   Use a default `patientId = 1` for demonstration purposes (as the mapping from User ID to Patient ID is not currently available in the frontend context).

### Technical Details
*   **Data Fetching**: Use `@tanstack/react-query` with `patientService.getVitals(patientId)`.
*   **Charts**: Use `recharts` for responsive and animated charts.
*   **UI**: Use Tailwind CSS for styling, matching the existing "medical" and "soft" design language.
*   **Icons**: Use `lucide-react`.

### Verification
*   I will verify the changes by checking the code structure and ensuring all imports are correct.
*   Since I cannot run the frontend to visually verify, I will ensure the code logic handles data fetching states (loading, error, empty data) gracefully.
