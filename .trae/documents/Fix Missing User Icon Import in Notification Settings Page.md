I will fix the `ReferenceError: User is not defined` by importing the missing `User` icon from `lucide-react` in the notification settings page.

### Changes
1.  **Update Imports**: Add `User` to the import list from `lucide-react` in `frontend/src/app/dashboard/admin/settings/notifications/page.tsx`.

### File to Modify
*   `frontend/src/app/dashboard/admin/settings/notifications/page.tsx`
