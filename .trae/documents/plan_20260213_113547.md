I will implement the Vital Signs feature in the Nurse Panel to match the Admin Panel's structure (Charts, Detailed History, Comprehensive Form) while adopting a Mobile-First and User-Friendly UI.

### 1. New "Vital Signs" Dashboard Page
*   **Create Page:** `frontend/src/app/nurse-portal/vital-signs/page.tsx`
*   **Functionality:**
    *   Lists all assigned patients with a snapshot of their **latest vital signs**.
    *   Visual indicators for "Stable" vs "Critical" status.
    *   "Quick Add" button for each patient to rapidly record vitals.
*   **UI/UX:** Card-based layout optimized for touch, with clear status colors.

### 2. Upgrade Patient Vitals View (`VitalsManager`)
*   **Refactor:** `frontend/src/components/nurse-portal/vitals-manager.tsx`
*   **Enhancements:**
    *   **Add History List:** Implement a detailed history view (similar to Admin's table) but using a **Responsive Card List** for mobile and Table for desktop.
    *   **Add Compliance Logic:** specific logic from Admin Panel (e.g., "On Time", "Late", "Next Due Calculation").
    *   **Improve Charts:** Ensure charts are responsive and match the Admin's data visualization depth.

### 3. Comprehensive Vitals Form
*   **Create Component:** `frontend/src/components/nurse-portal/vital-signs-form.tsx`
*   **Features:**
    *   Full field support (BP, Pulse, Temp, SpO2, Resp, GCS, Notes) like Admin.
    *   **Validation & Warnings:** Re-implement the clinical warnings (e.g., abnormal values) from the Admin form.
    *   **UI:** Glassmorphism modal design, large touch-friendly inputs.

### 4. Navigation & Quick Access
*   **Sidebar:** Update `frontend/src/components/nurse-portal/sidebar.tsx` to include "Vital Signs" (علائم حیاتی).
*   **Dashboard:** Update `frontend/src/app/nurse-portal/page.tsx` to add a "Quick Vital Signs Registration" button in the Quick Access section.

### 5. Implementation Steps
1.  Create the `VitalSignsForm` and `VitalSignsList` components for Nurse Portal.
2.  Refactor `VitalsManager` to use these new components and include the "Next Due" and "Compliance" logic.
3.  Create the `/nurse-portal/vital-signs` main page.
4.  Update Sidebar and Dashboard navigation.
