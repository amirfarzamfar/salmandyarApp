I will implement the **Service Registration** and **Reminder Registration** modules in the Nurse Portal with full CRUD capabilities, matching the Admin panel's structure but with a mobile-first design.

### Backend Implementation (Enable CRUD)

1. **DTO Updates**:

   * Add `UpdateCareServiceDto` to `MedicalDtos.cs` for editing services.

   * Add `UpdateServiceReminderDto` to `ServiceReminderDtos.cs` for editing reminders.
2. **Service Layer**:

   * Update `IPatientService` and `PatientService` to include `UpdateCareServiceAsync` and `DeleteCareServiceAsync`.
3. **Controllers**:

   * Update `PatientsController` to add `PUT` (Update) and `DELETE` endpoints for services.

   * Update `ServiceRemindersController` to add `PUT` (Update) endpoint for reminders.

### Frontend Implementation (Nurse Portal)

1. **Service Layer**:

   * Update `patient.service.ts` to include `updateService` and `deleteService` methods.

   * Update `service-reminder.service.ts` to include `update` method.
2. **UI Components (`ServiceTracker`)**:

   * **Tabs**: Add a "Reminders" tab alongside the existing "Services" tab.

   * **Mobile-First Forms**:

     * Replace the current simple inputs with robust `react-hook-form` implementations.

     * Implement **Service Registration Form** with service catalog selection (fetched from backend).

     * Implement **Reminder Registration Form** with date/time picker and recipient selection.

   * **CRUD Actions**:

     * Add **Edit** and **Delete** buttons to each service and reminder card.

     * Implement the logic to handle these actions, updating the UI optimistically or after success.

   * **Design**:

     * Use `PortalCard` and existing design tokens for a consistent, touch-friendly mobile experience.

     * Use modals/bottom-sheets for forms to maintain context.

